// page-password.example.js — REMOVED
// This example file previously provided a simple front-end password for the
// (now removed) view-messages page. It has been replaced with a placeholder as
// part of the project cleanup. If you intend to protect another page using a
// front-end password for local testing, create a new file and keep it out of
// version control.

// No-op placeholder
if (typeof console !== 'undefined') console.info('page-password.example.js placeholder (removed)');
